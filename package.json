{
  "name": "gangtalk-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "clean": "rimraf dist",
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host --port 4173",
    "start": "vite preview --host --port 4173",
    "deploy:hosting": "npm run clean && npm run build && firebase deploy --only hosting --debug",
    "deploy:functions": "cd functions && npm i && firebase deploy --only functions --debug",
    "deploy:all": "npm run clean && npm run build && firebase deploy --only hosting,functions --debug"
  },
  "dependencies": {
    "firebase": "^10.12.5",
    "vue": "^3.5.18",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "rimraf": "^5.0.5",
    "vite": "^7.1.3"
  }
}
